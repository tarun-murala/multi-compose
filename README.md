# multi-compose

This Docker Compose consists of
1. Jenkins
2. Jira
3. BitBucket
4. GitLab
5. GitLab Runner

Use the following commands to spin up the container(s)

```
   docker-compose up -d
```

To Shutdown

```
   docker-compose down
```